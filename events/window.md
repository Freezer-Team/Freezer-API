# `window`

应用窗口状态发生变化时触发。

| 方法 | 类型 | 含义 |
| --- | --- | --- |
| `getAppRecord()` | 应用对象 | 窗口所属应用 |
| `isState()` | `boolean` | `true`：获得有效窗口；`false`：窗口消失 |

**阶段：** `NONE`  
**可取消：** 是。取消会阻止 Freezer 随后的解冻或冻结调度，不会回滚窗口状态。
