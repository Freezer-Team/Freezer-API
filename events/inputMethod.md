# `inputMethod`

输入法服务活动状态变化时触发。

| 方法 | 类型 | 含义 |
| --- | --- | --- |
| `getAppRecord()` | 应用对象 | 输入法服务所属应用 |
| `isState()` | `boolean` | `true`：输入法进入活动状态；`false`：最后一个输入法服务解除 |

**阶段：** `NONE`  
**可取消：** 是。取消会阻止活动时解冻或解除后重新冻结。
