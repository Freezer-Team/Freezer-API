# `audio`

检测到应用音频活动开始或结束时触发。

| 方法 | 类型 | 含义 |
| --- | --- | --- |
| `getAppRecord()` | 应用对象 | 音频活动所属应用 |
| `isPlaying()` | `boolean` | `true`：音频活动开始；`false`：音频活动结束 |

**阶段：** `NONE`  
**可取消：** 是。取消会阻止音频开始时解冻，或结束后安排冻结。
