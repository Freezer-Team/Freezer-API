# `startBroadcast`

系统接受广播开始处理时触发。

| 方法 | 类型 | 含义 |
| --- | --- | --- |
| `getAppRecord()` | 应用对象 | 广播接收者应用 |
| `getIntent()` | 意图对象 | android.content.Intent |

**阶段：** `NONE`  
**可取消：** 是。取消会阻止对应的解冻或重新冻结。
