# `packet`

冻结应用收到网络数据时触发。事件来源不提供某项值时可能返回 `-1`。

| 方法 | 类型 | 含义 |
| --- | --- | --- |
| `getProto()` | `int` | IP 版本：`4` 为 IPv4，`6` 为 IPv6，`-1` 为未知 |
| `getTargetUid()` | `int` | 接收数据的目标 UID |
| `getDataLen()` | `int` | 收到的数据长度；未知时为 `-1` |

**阶段：** `NONE`  
**可取消：** 是。取消只阻止 Freezer 临时解冻应用，不会丢弃网络包。
