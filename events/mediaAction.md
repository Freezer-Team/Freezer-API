# `mediaAction`

应用接收到媒体控制动作时触发，例如播放、下一首、上一首、跳转位置或音量调整。

| 方法 | 类型 | 含义 |
| --- | --- | --- |
| `getAppRecord()` | 应用对象 | 接收媒体动作的应用 |
| `getMethodName()` | `String` | 媒体动作 |

**阶段：** `NONE`  
**可取消：** 是。取消只阻止 Freezer 因媒体动作临时解冻应用，不会阻止媒体动作本身。
