# `autoFill`

自动填充服务活动状态变化时触发。

| 方法 | 类型 | 含义 |
| --- | --- | --- |
| `getAppRecord()` | 应用对象 | 自动填充服务所属应用 |
| `isState()` | `boolean` | `true`：服务进入活动状态；`false`：最后一个服务解除 |

**阶段：** `NONE`  
**可取消：** 是。取消会阻止对应的解冻或重新冻结。
