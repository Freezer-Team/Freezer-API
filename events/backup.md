# `backup`

应用备份状态变化时触发。

| 方法 | 类型 | 含义 |
| --- | --- | --- |
| `getAppRecord()` | 应用对象 | 自动填充服务所属应用 |
| `isState()` | `boolean` | `true`：应用开始备份数据；`false`：应用停止备份数据 |

**阶段：** `NONE`  
**可取消：** 是。取消会阻止对应的解冻或重新冻结。
